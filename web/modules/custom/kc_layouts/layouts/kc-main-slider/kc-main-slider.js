/**
 * @file
 * Custom behaviors for main slider layout.
 */

(function ($, Drupal, once, drupalSettings) {

  'use strict';

  Drupal.behaviors.kcMainSlider = {
    attach (context, settings) {

      console.log('module')
    }
  };

}(jQuery, Drupal, once, drupalSettings));
